pipeline {
    agent any

    stages {
     stage('git code') {
         git 'https://github.com/bharanirm/my-app.git'
             }
        stage('build') {
         def mvnHome=C:\apache-maven-3.5.3-bin\apache-maven-3.5.3
         bat(/"${mvnHome}\bin\mvn" -Dmaven.test.failure.ignore clean package/)
              }
             }
        }
